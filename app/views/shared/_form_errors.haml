- if defined?(object) and object.errors.any?
  .error_explanation
    %h4== #{pluralize(object.errors.count, "error")} prohibited this from being saved:
    %ul
      - object.errors.full_messages.each do |message|
        %li= message